/** Shared cluster colors - set A (left, arrives first) vs set B (right, arrives second).
 * Used by both the counting scene and the equation card so the two stay visually linked. Dots
 * fill with this same color (see Dot.tsx) - no separate pale "background" tint. */
export const COLOR_A = "var(--color-one)";
export const COLOR_B = "var(--color-ten)";
